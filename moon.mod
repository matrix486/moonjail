name = "matrix486/moonjail"

version = "0.1.0"

readme = "README.mbt.md"

repository = "https://github.com/matrix486/moonjail"

license = "Apache-2.0"

keywords = [ "linux", "sandbox", "seccomp", "landlock", "security" ]

description = "Linux least-privilege sandbox planning and runtime for MoonBit"

preferred_target = "native"

import {
  "moonbitlang/x@0.5.5",
}
